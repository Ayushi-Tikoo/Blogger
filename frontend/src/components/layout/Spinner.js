import React, { Fragment } from 'react';
import { WaveTopBottomLoading } from 'react-loadingg';

const Spinner = () => (
  <Fragment>
    <WaveTopBottomLoading color='#8d4001' size={'large'} />
  </Fragment>
);

export default Spinner;
